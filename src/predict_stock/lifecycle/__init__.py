"""Model lifecycle (Phase 9): registry with statuses, provenance of every recommendation, monitoring, retrain triggers, shadow challengers, promotion by rule and by hand,
rollback, and the use of accumulated outcomes. Discipline over luck: a model is replaced only by evidence gathered AFTER both were trained, never because one random attempt looked
better on data already used. See docs/model_lifecycle.md."""
